<h1 align = "center">Role</h1> </br>
<div align="center">
  <% @roles.each do |role| %>
  <ul class = "listing">
      <div class="row">
        <div class="well will_categories col-md-4 col-md-offset-4">
          <div class="product-title">
            <strong>
                <%= link_to "#{role.role_name}", role_path(role) %>
            </strong>
          </li></br>
          <li><small><%= pluralize(role.users.count, "user") %></small></li>
          </div>
        </div>
      </div>
  </ul>
  <% end %>
</div>
