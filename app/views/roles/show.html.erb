<h1 align="center"><%= "Role: " + @role.role_name %></h1>
<div align="center">
    <div class="row">
      <span class="badge"><%= link_to "Edit Role Name", edit_role_path(@role) %></span>
    </div>
</div>

<h2>description: <%= @role.role_description %></h2>

-----------------
<h4><strong>Users</strong></h4>
<% @role.users.each do |user| %>
  <strong><%= user.id %>:</strong>
  <strong><%= link_to "#{user.full_name}", user_path(user) %></strong> </br>

<% end %>

</br>

<%= link_to 'edit', edit_role_path(@role) %>
<%= link_to 'back', root_path %>
