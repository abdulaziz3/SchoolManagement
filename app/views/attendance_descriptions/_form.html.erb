<%= form_for(@attendance_description) do |f| %>
  <% if @attendance_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendance_description.errors.count, "error") %> prohibited this attendance_description from being saved:</h2>

      <ul>
      <% @attendance_description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :att_status %><br>
    <%= f.text_field :att_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
