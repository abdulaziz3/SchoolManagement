<table class="table table-hover">
		<thead>
    		<tr>
			<th data-field="#" data-sortable="true"> ID</th>
			<th data-field="Last name" data-sortable="true">Last Name</th>
      <th data-field="First name" data-sortable="true">First Name</th>
			<th data-field="Nationality" data-sortable="true">nationality</th>
			<th data-field="national_id" data-sortable="true">national_id</th>
			<th data-field="E-mail" data-sortable="true">E-mail</th>
			<th data-field="Role" data-sortable="true">Role</th>
    		</tr>
  		</thead>

 	 	<tbody>
   			 <% @users.each do |user| %>
     	 		<tr data-link="<%= user_path(user) %>">
					<td><%= user.id %></td>
      			 		<td><%= user.l_name %></td>
      			 		<td><%= user.f_name %></td>
								<td><%= user.nationality %></td>
								<td><%= user.national_id %></td>
								<td><%= user.email %></td>
								<td><%= user.role %></td>

      			</tr>
   	 		<% end %>
		</tbody>
	</table>
<%= link_to 'Home Page', root_path, class: "btn btn-md btn-success pull-right" %>
